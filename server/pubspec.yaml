name: server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  args: ^2.0.0
  collection: ^1.17.0
  dotenv: ^4.0.1
  shelf: ^1.4.0
  shelf_router: ^1.0.0
  sqlite3: ^1.9.1

dev_dependencies:
  http: ^0.13.0
  lints: ^2.0.1
  test: ^1.22.2
